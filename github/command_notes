# How to create a new branch and push in git command:
git checkout -b feature_branch_name
git push -u origin feature_branch_name

# How to amend a commit:
git commit --amend -m "commit message"

# This will unstage all files you might have staged with git add:
git reset

# How to show the original git path:
git remote show origin

# How to stash on git:
git stash
# How to pop a stash:
git stash pop

# How to discard unstaged changes
# Discarding local changes (permanently) to a file. 
git checkout -- <file>

# How to delete a local branch, using D to force delete
git branch -d <branch_name>
# How to delete a remote branch 
git push <remote_name> --delete <branch_name>
# How to checkout a branch from a fork repo, and using branch name directly would not work
git checkout origin/<branch_name>
